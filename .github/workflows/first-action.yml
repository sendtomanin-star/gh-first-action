name: frist-workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
      - run: |
             echo "Print greeting"
             echo "Print Good Bye!"
